{
  "Baseline Coverage Alcohol Ban": {
    "value": 0.05,
    "description": "The baseline coverage for the intervention",
    "paths": [
      "$.nodes[?(@.id=='Rzb0zAne')].generate_array.parameters.value",
      "$.nodes[?(@.id=='ERfKe83x')].generate_array.parameters.start_value",
      "$.nodes[?(@.id=='ERfKe83x')].generate_array.parameters.fill_before"
    ],
    "category": "Coverage"
  },
  "Target Coverage Alcohol Ban": {
    "value": 0.05,
    "description": "The target coverage for the intervention",
    "paths": [
      "$.nodes[?(@.id=='ERfKe83x')].generate_array.parameters.end_value",
      "$.nodes[?(@.id=='ERfKe83x')].generate_array.parameters.fill_after"
    ],
    "category": "Coverage"
  },
  "Scale-Up Start Year Alcohol Ban": {
    "value": 1,
    "description": "The index when scale-up starts for this intervention, where 0 == the first year of the time horizon",
    "paths": [
      "$.nodes[?(@.id=='ERfKe83x')].generate_array.parameters.start_index"
    ],
    "category": "Coverage"
  },
  "Scale-Up Final Year Alcohol Ban": {
    "value": 2,
    "description": "The index when scale-up ends for this intervention, where 0 == the first year of the time horizon",
    "paths": [
      "$.nodes[?(@.id=='ERfKe83x')].generate_array.parameters.end_index"
    ],
    "category": "Coverage"
  },
  "Baseline Coverage Alcohol Restrict": {
    "value": 0.05,
    "description": "The baseline coverage for the intervention",
    "paths": [
      "$.nodes[?(@.id=='wkswo620')].generate_array.parameters.value",
      "$.nodes[?(@.id=='XaMz5uch')].generate_array.parameters.start_value",
      "$.nodes[?(@.id=='XaMz5uch')].generate_array.parameters.fill_before"
    ],
    "category": "Coverage"
  },
  "Target Coverage Alcohol Restrict": {
    "value": 0.05,
    "description": "The target coverage for the intervention",
    "paths": [
      "$.nodes[?(@.id=='XaMz5uch')].generate_array.parameters.end_value",
      "$.nodes[?(@.id=='XaMz5uch')].generate_array.parameters.fill_after"
    ],
    "category": "Coverage"
  },
  "Scale-Up Start Year Alcohol Restrict": {
    "value": 1,
    "description": "The index when scale-up starts for this intervention, where 0 == the first year of the time horizon",
    "paths": [
      "$.nodes[?(@.id=='XaMz5uch')].generate_array.parameters.start_index"
    ],
    "category": "Coverage"
  },
  "Scale-Up Final Year Alcohol Restrict": {
    "value": 2,
    "description": "The index when scale-up ends for this intervention, where 0 == the first year of the time horizon",
    "paths": [
      "$.nodes[?(@.id=='XaMz5uch')].generate_array.parameters.end_index"
    ],
    "category": "Coverage"
  },
  "Baseline Coverage Alcohol Counsel": {
    "value": 0.05,
    "description": "The baseline coverage for the intervention",
    "paths": [
      "$.nodes[?(@.id=='jaYSlCkx')].generate_array.parameters.value",
      "$.nodes[?(@.id=='hS1pbjZJ')].generate_array.parameters.start_value",
      "$.nodes[?(@.id=='hS1pbjZJ')].generate_array.parameters.fill_before"
    ],
    "category": "Coverage"
  },
  "Target Coverage Alcohol Counsel": {
    "value": 0.05,
    "description": "The target coverage for the intervention",
    "paths": [
      "$.nodes[?(@.id=='hS1pbjZJ')].generate_array.parameters.end_value",
      "$.nodes[?(@.id=='hS1pbjZJ')].generate_array.parameters.fill_after"
    ],
    "category": "Coverage"
  },
  "Scale-Up Start Year Alcohol Counsel": {
    "value": 1,
    "description": "The index when scale-up starts for this intervention, where 0 == the first year of the time horizon",
    "paths": [
      "$.nodes[?(@.id=='hS1pbjZJ')].generate_array.parameters.start_index"
    ],
    "category": "Coverage"
  },
  "Scale-Up Final Year Alcohol Counsel": {
    "value": 2,
    "description": "The index when scale-up ends for this intervention, where 0 == the first year of the time horizon",
    "paths": [
      "$.nodes[?(@.id=='hS1pbjZJ')].generate_array.parameters.end_index"
    ],
    "category": "Coverage"
  }
}