# Configuration for Physical Inactivity P1 Scenario
metadata:
  label: "Appendix 3 - Physical Inactivity P1 Scenario"
  description: |
    Physical activity advice intervention that increases coverage of brief counseling in primary care.
    This scenario modifies coverage parameters from baseline to target levels over a specified scale-up period.
    The intervention reduces prevalence of physical inactivity, which changes the population attributable fraction and subsequently affects diabetes incidence rates.
  authors: []

# Components to include (from build/components/)
components:
  - diabetes_base.json
  - physical_inactivity_p1.json

# Override specific values for P1 scenario
overrides:
  "Target Coverage Physical Inactivity Advice":
    value: 0.95

# Output file path (relative to build directory)
output: ../scenarios/physical_inactivity_p1.json
