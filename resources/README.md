# Diabetes clinical resource module

This directory owns the clinical resource package for the disease model. The NCD client mirrors this directory into `public/models` so the browser can load the package and service graphs at runtime.
